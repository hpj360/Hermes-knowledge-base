# Hermes KB 前端环境变量模板
# 复制此文件为 .env 并填入你的值
# 绝对不要将 .env 提交到版本控制

# ---------------------------------------------------------------------------
# API 后端地址
# ---------------------------------------------------------------------------
# 默认空串即同源部署（前端与后端同一域名/端口）。
# 若前端独立部署到 CDN 或不同域名（如 https://kb.example.com 调 https://api.example.com），
# 设置为后端完整地址（不含尾部斜杠）。
VITE_API_BASE=http://localhost:8000

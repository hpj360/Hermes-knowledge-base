# M4.3 UGC 调酒研究室实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用户可创作自定义配方（UGC），经审核流程后发布到实验室匹配。支持基于已有配方创作"变体"并关联展示。

**Architecture:** UGC 配方作为 Document（category=recipe, source=ugc）。审核状态机 draft→pending→published/rejected 复用 M4.2 已扩展的 status 字段。变体关联用独立表 RecipeVariant。配方编辑器独立页面 recipe-editor.html。

**Tech Stack:** Python 3.11 / FastAPI / SQLModel / SQLite / 原生 HTML+CSS+JS

**前置依赖:** M4.2 已完成（Document 的 source/status/verified 字段已就绪）

---

## 文件结构

### 新建文件（后端）
- `src/hermes_kb/recipe_crud.py` — UGC 配方 CRUD + 审核状态机
- `src/hermes_kb/recipe_variants.py` — 变体关联管理

### 新建文件（测试）
- `tests/test_kb/test_lab_ugc.py` — M4.3 测试

### 新建文件（前端）
- `design/mockup/recipe-editor.html` — 配方编辑器页面

### 修改文件
- `src/hermes_kb/models.py` — 新增 RecipeVariant 表
- `src/hermes_kb/database.py` — init_db 确保新表创建
- `src/hermes_kb/app.py` — 新增 6 个端点
- `src/hermes_kb/ingredients.py` — 增加 list_all_for_editor() 辅助
- `design/mockup/_nav.js` — 导航增加"创作"入口
- `design/mockup/doc-detail.html` — 配方详情增加变体区
- `design/mockup/dashboard.html` — 增加待审核配方队列
- `design/mockup/_components.css` — 追加编辑器/变体组件样式

---

## Task 1: RecipeVariant 数据模型

**Files:**
- Modify: `src/hermes_kb/models.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_kb/test_lab_ugc.py`：

```python
"""M4.3 UGC 调酒研究室测试。"""
from __future__ import annotations

import pytest
from sqlmodel import select


def test_recipe_variant_model(tmp_db):
    """RecipeVariant 表可创建并写入。"""
    from hermes_kb.models import RecipeVariant
    from hermes_kb.database import get_session

    with get_session() as session:
        v = RecipeVariant(
            base_doc_id="doc_base001",
            variant_doc_id="doc_variant001",
            variant_note="辛辣版，增加苦精",
        )
        session.add(v)
        session.commit()
        session.refresh(v)
        assert v.id is not None
        assert v.base_doc_id == "doc_base001"
        assert v.variant_doc_id == "doc_variant001"
        assert v.variant_note == "辛辣版，增加苦精"
        assert v.created_at is not None
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_recipe_variant_model -v`

Expected: FAIL with "cannot import name 'RecipeVariant'"

- [ ] **Step 3: 实现 RecipeVariant 模型**

在 `/workspace/src/hermes_kb/models.py` 末尾（MissingIngredientStats 之后）追加：

```python


class RecipeVariant(SQLModel, table=True):
    """M4.3：配方变体关联。"""

    id: int | None = Field(default=None, primary_key=True)
    base_doc_id: str = Field(index=True, max_length=64)  # 原配方
    variant_doc_id: str = Field(index=True, max_length=64)  # 变体配方
    variant_note: str = Field(default="", max_length=200)  # 变体说明
    created_at: datetime = Field(default_factory=_utcnow)
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_recipe_variant_model -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/models.py tests/test_kb/test_lab_ugc.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ugc): RecipeVariant 数据模型

- 变体关联表（base_doc_id/variant_doc_id/variant_note）
- 支撑基于已有配方创作变体"
```

---

## Task 2: UGC 配方 CRUD + 审核状态机 — recipe_crud.py

**Files:**
- Create: `src/hermes_kb/recipe_crud.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ugc.py` 追加：

```python


def test_create_ugc_recipe(tmp_db):
    """创建 UGC 配方（draft 状态）。"""
    from hermes_kb.recipe_crud import create_recipe

    result = create_recipe(
        title="我的特调",
        ingredients=["金酒", "柠檬汁", "蜂蜜"],
        content="# 我的特调\n\n## 配方\n- 金酒 50ml\n- 柠檬汁 20ml\n- 蜂蜜 15ml",
        base_spirit="gin",
        difficulty="easy",
        season="spring",
    )
    assert result["doc_id"] is not None
    assert result["status"] == "draft"

    from hermes_kb.models import Document
    from hermes_kb.database import get_session

    with get_session() as session:
        doc = session.get(Document, result["doc_id"])
        assert doc.title == "我的特调"
        assert doc.category == "recipe"
        assert doc.source == "ugc"
        assert doc.verified is False
        assert doc.status == "draft"
        assert doc.season == "spring"


def test_submit_recipe(tmp_db):
    """提交审核（draft → pending）。"""
    from hermes_kb.recipe_crud import create_recipe, submit_recipe
    from hermes_kb.models import Document
    from hermes_kb.database import get_session

    created = create_recipe(
        title="待审核配方",
        ingredients=["金酒"],
        content="# 待审核\n\n## 配方\n- 金酒 50ml",
        base_spirit="gin",
        difficulty="easy",
    )
    doc_id = created["doc_id"]

    ok = submit_recipe(doc_id)
    assert ok is True

    with get_session() as session:
        doc = session.get(Document, doc_id)
        assert doc.status == "pending"


def test_approve_recipe(tmp_db):
    """审核通过（pending → published, verified=True）。"""
    from hermes_kb.recipe_crud import create_recipe, submit_recipe, approve_recipe
    from hermes_kb.models import Document
    from hermes_kb.database import get_session

    created = create_recipe(
        title="将通过",
        ingredients=["金酒"],
        content="# 将通过\n\n## 配方\n- 金酒 50ml",
        base_spirit="gin",
        difficulty="easy",
    )
    submit_recipe(created["doc_id"])

    ok = approve_recipe(created["doc_id"])
    assert ok is True

    with get_session() as session:
        doc = session.get(Document, created["doc_id"])
        assert doc.status == "published"
        assert doc.verified is True


def test_reject_recipe(tmp_db):
    """审核驳回（pending → rejected）。"""
    from hermes_kb.recipe_crud import create_recipe, submit_recipe, reject_recipe
    from hermes_kb.models import Document
    from hermes_kb.database import get_session

    created = create_recipe(
        title="将驳回",
        ingredients=["金酒"],
        content="# 将驳回\n\n## 配方\n- 金酒 50ml",
        base_spirit="gin",
        difficulty="easy",
    )
    submit_recipe(created["doc_id"])

    ok = reject_recipe(created["doc_id"], reason="材料比例不合理")
    assert ok is True

    with get_session() as session:
        doc = session.get(Document, created["doc_id"])
        assert doc.status == "rejected"
        assert doc.verified is False


def test_update_recipe(tmp_db):
    """编辑配方（仅 draft 状态可编辑）。"""
    from hermes_kb.recipe_crud import create_recipe, update_recipe, submit_recipe
    from hermes_kb.models import Document
    from hermes_kb.database import get_session

    created = create_recipe(
        title="原配方",
        ingredients=["金酒"],
        content="# 原配方\n\n## 配方\n- 金酒 50ml",
        base_spirit="gin",
        difficulty="easy",
    )
    # draft 状态可编辑
    ok = update_recipe(
        created["doc_id"],
        title="改后配方",
        ingredients=["金酒", "柠檬汁"],
        content="# 改后配方\n\n## 配方\n- 金酒 50ml\n- 柠檬汁 20ml",
    )
    assert ok is True

    with get_session() as session:
        doc = session.get(Document, created["doc_id"])
        assert doc.title == "改后配方"

    # pending 状态不可编辑
    submit_recipe(created["doc_id"])
    ok2 = update_recipe(
        created["doc_id"],
        title="再改",
        ingredients=["金酒"],
        content="# 再改",
    )
    assert ok2 is False
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_create_ugc_recipe tests/test_kb/test_lab_ugc.py::test_submit_recipe tests/test_kb/test_lab_ugc.py::test_approve_recipe tests/test_kb/test_lab_ugc.py::test_reject_recipe tests/test_kb/test_lab_ugc.py::test_update_recipe -v`

Expected: FAIL with "No module named 'hermes_kb.recipe_crud'"

- [ ] **Step 3: 实现 recipe_crud.py**

创建 `/workspace/src/hermes_kb/recipe_crud.py`：

```python
"""UGC 配方 CRUD + 审核状态机（M4.3）。

状态机：draft → pending → published / rejected
- draft: 用户编辑中，仅自己可见
- pending: 提交审核，进入审核队列
- published: 审核通过，进实验室匹配（verified=True）
- rejected: 审核驳回，附驳回理由
"""
from __future__ import annotations

from typing import Any

from sqlmodel import select

from hermes_kb.database import get_session
from hermes_kb.models import Document
from hermes_kb.rag import ImportService


def create_recipe(
    title: str,
    ingredients: list[str],
    content: str,
    base_spirit: str = "",
    difficulty: str = "easy",
    season: str | None = None,
) -> dict[str, Any]:
    """创建 UGC 配方（draft 状态）。

    Returns:
        {doc_id, status, title}
    """
    importer = ImportService()
    result = importer.import_text(
        content=content,
        title=title,
        source_type="ugc",
        file_type="md",
    )
    doc_id = result.get("doc_id")
    if doc_id:
        with get_session() as session:
            doc = session.get(Document, doc_id)
            if doc:
                doc.category = "recipe"
                doc.source = "ugc"
                doc.source_id = f"ugc-{doc_id}"
                doc.verified = False
                doc.status = "draft"
                if season:
                    doc.season = season
                session.add(doc)
                session.commit()
    return {"doc_id": doc_id, "status": "draft", "title": title}


def update_recipe(
    doc_id: str,
    title: str | None = None,
    ingredients: list[str] | None = None,
    content: str | None = None,
    season: str | None = None,
) -> bool:
    """编辑配方（仅 draft 状态可编辑）。"""
    with get_session() as session:
        doc = session.get(Document, doc_id)
        if not doc or doc.status != "draft":
            return False
        if title is not None:
            doc.title = title
        if content is not None:
            doc.content = content
        if season is not None:
            doc.season = season
        # 注意：ingredients 更新需重新分片，此处仅更新 content
        # 若需更新 ingredients，应重新 import_text
        session.add(doc)
        session.commit()
        return True


def submit_recipe(doc_id: str) -> bool:
    """提交审核（draft → pending）。"""
    with get_session() as session:
        doc = session.get(Document, doc_id)
        if not doc or doc.status != "draft":
            return False
        doc.status = "pending"
        session.add(doc)
        session.commit()
        return True


def approve_recipe(doc_id: str) -> bool:
    """审核通过（pending → published, verified=True）。"""
    with get_session() as session:
        doc = session.get(Document, doc_id)
        if not doc or doc.status != "pending":
            return False
        doc.status = "published"
        doc.verified = True
        session.add(doc)
        session.commit()
        return True


def reject_recipe(doc_id: str, reason: str = "") -> bool:
    """审核驳回（pending → rejected）。"""
    with get_session() as session:
        doc = session.get(Document, doc_id)
        if not doc or doc.status != "pending":
            return False
        doc.status = "rejected"
        doc.verified = False
        # reason 存到 source_path 字段（复用）或扩展字段
        if reason:
            doc.source_path = f"reject_reason: {reason}"
        session.add(doc)
        session.commit()
        return True


def list_pending_recipes(limit: int = 20) -> list[dict[str, Any]]:
    """列出待审核配方。"""
    with get_session() as session:
        docs = session.exec(
            select(Document)
            .where(Document.category == "recipe", Document.status == "pending")
            .limit(limit)
        ).all()
        return [
            {
                "doc_id": d.doc_id,
                "title": d.title,
                "source": d.source,
                "status": d.status,
                "created_at": d.created_at.isoformat() if d.created_at else None,
            }
            for d in docs
        ]
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_create_ugc_recipe tests/test_kb/test_lab_ugc.py::test_submit_recipe tests/test_kb/test_lab_ugc.py::test_approve_recipe tests/test_kb/test_lab_ugc.py::test_reject_recipe tests/test_kb/test_lab_ugc.py::test_update_recipe -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/recipe_crud.py tests/test_kb/test_lab_ugc.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ugc): UGC 配方 CRUD + 审核状态机

- create_recipe(): 创建（draft）
- update_recipe(): 编辑（仅 draft）
- submit_recipe(): 提交审核（draft → pending）
- approve_recipe(): 审核通过（pending → published, verified=True）
- reject_recipe(): 审核驳回（pending → rejected）
- list_pending_recipes(): 待审核列表"
```

---

## Task 3: 变体关联管理 — recipe_variants.py

**Files:**
- Create: `src/hermes_kb/recipe_variants.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ugc.py` 追加：

```python


@pytest.fixture
def base_and_variant(tmp_db):
    """创建一个原配方和一个变体。"""
    from hermes_kb.recipe_crud import create_recipe

    base = create_recipe(
        title="原版马天尼",
        ingredients=["金酒", "味美思", "橄榄"],
        content="# 原版马天尼\n\n## 配方\n- 金酒 60ml\n- 味美思 10ml\n- 橄榄 1颗",
        base_spirit="gin",
        difficulty="easy",
    )
    variant = create_recipe(
        title="辛辣马天尼",
        ingredients=["金酒", "味美思", "苦精"],
        content="# 辛辣马天尼\n\n## 配方\n- 金酒 60ml\n- 味美思 10ml\n- 苦精 2滴",
        base_spirit="gin",
        difficulty="medium",
    )
    return base, variant


def test_create_variant_link(base_and_variant):
    """创建变体关联。"""
    from hermes_kb.recipe_variants import create_variant_link, get_variants

    base, variant = base_and_variant
    ok = create_variant_link(
        base_doc_id=base["doc_id"],
        variant_doc_id=variant["doc_id"],
        variant_note="增加苦精的辛辣版",
    )
    assert ok is True

    variants = get_variants(base["doc_id"])
    assert len(variants) == 1
    assert variants[0]["variant_doc_id"] == variant["doc_id"]
    assert variants[0]["variant_note"] == "增加苦精的辛辣版"
    assert variants[0]["variant_title"] == "辛辣马天尼"


def test_get_base_recipe(base_and_variant):
    """查询变体的原配方。"""
    from hermes_kb.recipe_variants import create_variant_link, get_base_recipe

    base, variant = base_and_variant
    create_variant_link(base["doc_id"], variant["doc_id"], "测试")

    base_info = get_base_recipe(variant["doc_id"])
    assert base_info is not None
    assert base_info["base_doc_id"] == base["doc_id"]
    assert base_info["base_title"] == "原版马天尼"


def test_create_variant_duplicate(base_and_variant):
    """重复创建变体关联返回 False。"""
    from hermes_kb.recipe_variants import create_variant_link

    base, variant = base_and_variant
    create_variant_link(base["doc_id"], variant["doc_id"], "第一次")
    ok = create_variant_link(base["doc_id"], variant["doc_id"], "第二次")
    assert ok is False
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_create_variant_link tests/test_kb/test_lab_ugc.py::test_get_base_recipe tests/test_kb/test_lab_ugc.py::test_create_variant_duplicate -v`

Expected: FAIL with "No module named 'hermes_kb.recipe_variants'"

- [ ] **Step 3: 实现 recipe_variants.py**

创建 `/workspace/src/hermes_kb/recipe_variants.py`：

```python
"""配方变体关联管理（M4.3）。"""
from __future__ import annotations

from typing import Any

from sqlmodel import select

from hermes_kb.database import get_session
from hermes_kb.models import Document, RecipeVariant


def create_variant_link(
    base_doc_id: str, variant_doc_id: str, variant_note: str = ""
) -> bool:
    """创建变体关联（已存在则返回 False）。"""
    with get_session() as session:
        # 检查是否已存在
        existing = session.exec(
            select(RecipeVariant).where(
                RecipeVariant.base_doc_id == base_doc_id,
                RecipeVariant.variant_doc_id == variant_doc_id,
            )
        ).first()
        if existing:
            return False

        # 检查两个 doc 是否存在
        base_doc = session.get(Document, base_doc_id)
        variant_doc = session.get(Document, variant_doc_id)
        if not base_doc or not variant_doc:
            return False

        link = RecipeVariant(
            base_doc_id=base_doc_id,
            variant_doc_id=variant_doc_id,
            variant_note=variant_note,
        )
        session.add(link)
        session.commit()
        return True


def get_variants(base_doc_id: str) -> list[dict[str, Any]]:
    """查询某配方的所有变体。"""
    with get_session() as session:
        links = session.exec(
            select(RecipeVariant).where(
                RecipeVariant.base_doc_id == base_doc_id
            )
        ).all()
        result = []
        for link in links:
            variant_doc = session.get(Document, link.variant_doc_id)
            result.append(
                {
                    "variant_doc_id": link.variant_doc_id,
                    "variant_title": variant_doc.title if variant_doc else "(已删除)",
                    "variant_note": link.variant_note,
                    "created_at": link.created_at.isoformat()
                    if link.created_at
                    else None,
                }
            )
        return result


def get_base_recipe(variant_doc_id: str) -> dict[str, Any] | None:
    """查询变体的原配方。"""
    with get_session() as session:
        link = session.exec(
            select(RecipeVariant).where(
                RecipeVariant.variant_doc_id == variant_doc_id
            )
        ).first()
        if not link:
            return None
        base_doc = session.get(Document, link.base_doc_id)
        return {
            "base_doc_id": link.base_doc_id,
            "base_title": base_doc.title if base_doc else "(已删除)",
            "variant_note": link.variant_note,
        }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_create_variant_link tests/test_kb/test_lab_ugc.py::test_get_base_recipe tests/test_kb/test_lab_ugc.py::test_create_variant_duplicate -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/recipe_variants.py tests/test_kb/test_lab_ugc.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ugc): 变体关联管理

- create_variant_link(): 创建关联（去重）
- get_variants(): 查询配方的变体列表
- get_base_recipe(): 查询变体的原配方"
```

---

## Task 4: API 端点 — UGC CRUD + 审核 + 变体

**Files:**
- Modify: `src/hermes_kb/app.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ugc.py` 追加：

```python


def test_api_create_recipe(client):
    """POST /api/lab/recipes 创建 UGC 配方。"""
    resp = client.post("/api/lab/recipes", json={
        "title": "API 特调",
        "ingredients": ["金酒", "柠檬汁"],
        "content": "# API 特调\n\n## 配方\n- 金酒 50ml\n- 柠檬汁 20ml",
        "base_spirit": "gin",
        "difficulty": "easy",
        "season": "summer",
    })
    assert resp.status_code == 200
    data = resp.json()
    assert data["status"] == "draft"
    assert data["doc_id"] is not None


def test_api_recipe_lifecycle(client):
    """UGC 配方完整生命周期：创建→提交→通过。"""
    # 创建
    created = client.post("/api/lab/recipes", json={
        "title": "生命周期测试",
        "ingredients": ["金酒"],
        "content": "# 生命周期测试\n\n## 配方\n- 金酒 50ml",
        "base_spirit": "gin",
        "difficulty": "easy",
    }).json()
    doc_id = created["doc_id"]

    # 提交
    resp = client.post(f"/api/lab/recipes/{doc_id}/submit")
    assert resp.status_code == 200
    assert resp.json()["status"] == "pending"

    # 通过
    resp = client.post(f"/api/lab/recipes/{doc_id}/approve")
    assert resp.status_code == 200
    assert resp.json()["status"] == "ok"


def test_api_recipe_reject(client):
    """POST /api/lab/recipes/{doc_id}/reject 审核驳回。"""
    created = client.post("/api/lab/recipes", json={
        "title": "将被驳回",
        "ingredients": ["金酒"],
        "content": "# 将被驳回",
        "base_spirit": "gin",
        "difficulty": "easy",
    }).json()
    client.post(f"/api/lab/recipes/{created['doc_id']}/submit")

    resp = client.post(
        f"/api/lab/recipes/{created['doc_id']}/reject",
        json={"reason": "配方不完整"},
    )
    assert resp.status_code == 200
    assert resp.json()["status"] == "rejected"


def test_api_recipe_variants(client, base_and_variant):
    """GET /api/lab/recipes/{doc_id}/variants 查看变体。"""
    from hermes_kb.recipe_variants import create_variant_link

    base, variant = base_and_variant
    create_variant_link(base["doc_id"], variant["doc_id"], "测试变体")

    resp = client.get(f"/api/lab/recipes/{base['doc_id']}/variants")
    assert resp.status_code == 200
    data = resp.json()
    assert len(data["items"]) == 1
    assert data["items"][0]["variant_title"] == "辛辣马天尼"


def test_api_pending_recipes(client, base_and_variant):
    """GET /api/lab/recipes?status=pending 查看待审核。"""
    from hermes_kb.recipe_crud import submit_recipe

    base, _ = base_and_variant
    submit_recipe(base["doc_id"])

    resp = client.get("/api/lab/recipes", params={"status": "pending"})
    assert resp.status_code == 200
    data = resp.json()
    assert any(i["title"] == "原版马天尼" for i in data["items"])
```

注意：base_and_variant fixture 用的是 tmp_db 而非 client。需确认 client fixture 是否依赖 tmp_db（autouse）。如果 client 和 base_and_variant 共享同一 tmp_db，测试可过。若不行，调整 fixture 依赖。

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_api_create_recipe tests/test_kb/test_lab_ugc.py::test_api_recipe_lifecycle tests/test_kb/test_lab_ugc.py::test_api_recipe_reject tests/test_kb/test_lab_ugc.py::test_api_recipe_variants tests/test_kb/test_lab_ugc.py::test_api_pending_recipes -v`

Expected: FAIL with "404 Not Found" 或 "405 Method Not Allowed"

- [ ] **Step 3: 实现 API 端点**

注意：M4.2 已有 `GET /api/lab/recipes`（筛选）和 `POST /api/lab/recipes/{doc_id}/verify`（审核通过）。M4.3 新增 `POST /api/lab/recipes`（创建 UGC）会与 M4.2 的 GET 同路径不同方法，需确认 FastAPI 路由不冲突。

在 `/workspace/src/hermes_kb/app.py` 的 M4.2 端点之后追加（4 空格缩进）：

```python

    # -----------------------------------------------------------------------
    # M4.3：UGC 调酒研究室
    # -----------------------------------------------------------------------
    @app.post("/api/lab/recipes")
    async def lab_create_recipe(req: dict[str, Any]) -> dict[str, Any]:
        """创建 UGC 配方（draft 状态）。"""
        from hermes_kb.recipe_crud import create_recipe

        title = (req.get("title") or "").strip()
        content = req.get("content") or ""
        if not title or not content.strip():
            raise HTTPException(status_code=400, detail="title 和 content 必填")
        ingredients = req.get("ingredients") or []
        result = create_recipe(
            title=title,
            ingredients=ingredients,
            content=content,
            base_spirit=req.get("base_spirit", ""),
            difficulty=req.get("difficulty", "easy"),
            season=req.get("season"),
        )
        return result

    @app.put("/api/lab/recipes/{doc_id}")
    async def lab_update_recipe(doc_id: str, req: dict[str, Any]) -> dict[str, Any]:
        """编辑 UGC 配方（仅 draft 状态）。"""
        from hermes_kb.recipe_crud import update_recipe

        ok = update_recipe(
            doc_id,
            title=req.get("title"),
            ingredients=req.get("ingredients"),
            content=req.get("content"),
            season=req.get("season"),
        )
        if not ok:
            raise HTTPException(status_code=400, detail="仅 draft 状态可编辑")
        return {"doc_id": doc_id, "status": "ok"}

    @app.post("/api/lab/recipes/{doc_id}/submit")
    async def lab_submit_recipe(doc_id: str) -> dict[str, Any]:
        """提交审核（draft → pending）。"""
        from hermes_kb.recipe_crud import submit_recipe

        ok = submit_recipe(doc_id)
        if not ok:
            raise HTTPException(status_code=400, detail="仅 draft 状态可提交")
        return {"doc_id": doc_id, "status": "pending"}

    @app.post("/api/lab/recipes/{doc_id}/approve")
    async def lab_approve_recipe(doc_id: str) -> dict[str, Any]:
        """审核通过（pending → published）。"""
        from hermes_kb.recipe_crud import approve_recipe

        ok = approve_recipe(doc_id)
        if not ok:
            raise HTTPException(status_code=400, detail="仅 pending 状态可审核")
        return {"doc_id": doc_id, "status": "ok"}

    @app.post("/api/lab/recipes/{doc_id}/reject")
    async def lab_reject_recipe(doc_id: str, req: dict[str, Any]) -> dict[str, Any]:
        """审核驳回（pending → rejected）。"""
        from hermes_kb.recipe_crud import reject_recipe

        reason = req.get("reason", "")
        ok = reject_recipe(doc_id, reason=reason)
        if not ok:
            raise HTTPException(status_code=400, detail="仅 pending 状态可驳回")
        return {"doc_id": doc_id, "status": "rejected", "reason": reason}

    @app.get("/api/lab/recipes/{doc_id}/variants")
    async def lab_recipe_variants(doc_id: str) -> dict[str, Any]:
        """查看配方的变体列表。"""
        from hermes_kb.recipe_variants import get_variants

        items = get_variants(doc_id)
        return {"items": items, "count": len(items)}

    @app.post("/api/lab/recipes/{doc_id}/variant")
    async def lab_create_variant(doc_id: str, req: dict[str, Any]) -> dict[str, Any]:
        """创建变体（基于 doc_id 配方创作新配方并关联）。"""
        from hermes_kb.recipe_crud import create_recipe
        from hermes_kb.recipe_variants import create_variant_link

        variant_doc_id = req.get("variant_doc_id")
        variant_note = req.get("variant_note", "")
        if not variant_doc_id:
            raise HTTPException(status_code=400, detail="variant_doc_id 必填")
        ok = create_variant_link(doc_id, variant_doc_id, variant_note)
        if not ok:
            raise HTTPException(status_code=400, detail="关联已存在或配方不存在")
        return {"base_doc_id": doc_id, "variant_doc_id": variant_doc_id, "status": "ok"}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py::test_api_create_recipe tests/test_kb/test_lab_ugc.py::test_api_recipe_lifecycle tests/test_kb/test_lab_ugc.py::test_api_recipe_reject tests/test_kb/test_lab_ugc.py::test_api_recipe_variants tests/test_kb/test_lab_ugc.py::test_api_pending_recipes -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/app.py tests/test_kb/test_lab_ugc.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ugc): API 端点 UGC CRUD + 审核 + 变体

- POST /api/lab/recipes: 创建 UGC 配方
- PUT /api/lab/recipes/{doc_id}: 编辑（仅 draft）
- POST /api/lab/recipes/{doc_id}/submit: 提交审核
- POST /api/lab/recipes/{doc_id}/approve: 审核通过
- POST /api/lab/recipes/{doc_id}/reject: 审核驳回
- GET /api/lab/recipes/{doc_id}/variants: 变体列表
- POST /api/lab/recipes/{doc_id}/variant: 创建变体关联"
```

---

## Task 5: 前端 — recipe-editor.html 配方编辑器

**Files:**
- Create: `design/mockup/recipe-editor.html`
- Modify: `design/mockup/_nav.js`
- Modify: `design/mockup/_components.css`

- [ ] **Step 1: 追加编辑器样式**

在 `design/mockup/_components.css` 末尾追加：

```css


/* === M4.3 UGC 配方编辑器 === */
.recipe-editor {
  max-width: 720px; margin: 0 auto; padding: var(--sp-6);
}
.editor-field { margin-bottom: var(--sp-4); }
.editor-field label {
  display: block; font-size: var(--fs-sm); color: var(--ink-600);
  margin-bottom: var(--sp-1); font-weight: 500;
}
.editor-field input,
.editor-field select,
.editor-field textarea {
  width: 100%; padding: var(--sp-2) var(--sp-3);
  border: 1px solid var(--ink-200); border-radius: var(--r-md);
  font-size: var(--fs-sm); font-family: inherit;
}
.editor-field textarea { min-height: 120px; resize: vertical; }
.editor-field input:focus,
.editor-field select:focus,
.editor-field textarea:focus {
  outline: none; border-color: var(--brand-500);
}
.editor-ingredients {
  display: flex; flex-wrap: wrap; gap: var(--sp-2);
  padding: var(--sp-2); background: var(--ink-50);
  border-radius: var(--r-md); min-height: 48px;
}
.editor-actions {
  display: flex; gap: var(--sp-3); margin-top: var(--sp-5);
}
.btn-primary {
  background: var(--brand-700); color: #fff; border: none;
  padding: var(--sp-2) var(--sp-5); border-radius: var(--r-md);
  font-size: var(--fs-sm); cursor: pointer;
}
.btn-primary:hover { background: var(--brand-800); }
.btn-secondary {
  background: var(--ink-100); color: var(--ink-700); border: none;
  padding: var(--sp-2) var(--sp-5); border-radius: var(--r-md);
  font-size: var(--fs-sm); cursor: pointer;
}
.btn-secondary:hover { background: var(--ink-200); }
```

- [ ] **Step 2: 创建 recipe-editor.html**

创建 `/workspace/design/mockup/recipe-editor.html`：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>创作配方 · Hermes KB</title>
  <link rel="stylesheet" href="_tokens.css">
  <link rel="stylesheet" href="_components.css">
  <script src="_nav.js"></script>
</head>
<body>
  <div id="nav"></div>

  <main class="recipe-editor">
    <div class="page-kicker">Lab · 调酒研究室</div>
    <h1 class="serif">创作新配方</h1>
    <p class="meta">创作完成后提交审核，通过后进入实验室匹配</p>

    <div class="editor-field">
      <label>配方名 *</label>
      <input type="text" id="recipeTitle" placeholder="如：夏日清风">
    </div>

    <div class="editor-field">
      <label>基酒</label>
      <select id="recipeBaseSpirit">
        <option value="gin">金酒 Gin</option>
        <option value="rum">朗姆酒 Rum</option>
        <option value="vodka">伏特加 Vodka</option>
        <option value="tequila">龙舌兰 Tequila</option>
        <option value="whiskey">威士忌 Whiskey</option>
        <option value="brandy">白兰地 Brandy</option>
      </select>
    </div>

    <div class="editor-field">
      <label>难度</label>
      <select id="recipeDifficulty">
        <option value="easy">简单</option>
        <option value="medium">中等</option>
        <option value="hard">困难</option>
      </select>
    </div>

    <div class="editor-field">
      <label>季节</label>
      <select id="recipeSeason">
        <option value="">不限</option>
        <option value="spring">春季</option>
        <option value="summer">夏季</option>
        <option value="autumn">秋季</option>
        <option value="winter">冬季</option>
      </select>
    </div>

    <div class="editor-field">
      <label>材料（点击添加）</label>
      <div class="editor-ingredients" id="selectedIngredients">
        <span style="color:var(--ink-400);font-size:var(--fs-xs)">未选择材料</span>
      </div>
    </div>

    <div class="editor-field">
      <label>配方内容（Markdown）*</label>
      <textarea id="recipeContent" placeholder="# 配方名&#10;&#10;## 配方&#10;- 金酒 50ml&#10;- 柠檬汁 20ml&#10;&#10;## 步骤&#10;1. ..."></textarea>
    </div>

    <div class="editor-actions">
      <button class="btn-primary" onclick="saveRecipe('draft')">保存草稿</button>
      <button class="btn-secondary" onclick="saveRecipe('submit')">保存并提交审核</button>
    </div>

    <div id="saveResult" style="margin-top:var(--sp-4);"></div>
  </main>

  <script>
    var selected = {};

    // 材料选择器（简化版，复用 lab.html 的注册表概念）
    // TODO: 从 /api 获取材料列表，此处先用预设
    var COMMON_INGREDIENTS = ['金酒', '朗姆酒', '伏特加', '龙舌兰', '威士忌', '白兰地',
      '味美思', '金巴利', '君度', '苦精', '柠檬汁', '青柠汁', '糖浆', '苏打水', '汤力水',
      '橄榄', '橙皮', '薄荷叶', '樱桃'];

    function renderIngredientPicker() {
      // 渲染到 selectedIngredients 区域上方
      var html = COMMON_INGREDIENTS.map(function(name) {
        var cls = selected[name] ? 'chip-chip selected cat-base' : 'chip-chip cat-base';
        return '<button class="' + cls + '" onclick="toggleIng(\'' + name + '\')">' + name + '</button>';
      }).join('');
      var picker = '<div style="margin-bottom:var(--sp-2);display:flex;flex-wrap:wrap;gap:var(--sp-1);">' + html + '</div>';
      var sel = Object.keys(selected);
      var selHtml = sel.length > 0
        ? sel.map(function(n) { return '<span class="chip-chip selected">' + n + ' ×</span>'; }).join('')
        : '<span style="color:var(--ink-400);font-size:var(--fs-xs)">未选择材料</span>';
      document.getElementById('selectedIngredients').innerHTML = picker + '<div>' + selHtml + '</div>';
    }

    function toggleIng(name) {
      if (selected[name]) delete selected[name];
      else selected[name] = true;
      renderIngredientPicker();
    }

    function saveRecipe(action) {
      var title = document.getElementById('recipeTitle').value.trim();
      var content = document.getElementById('recipeContent').value.trim();
      if (!title || !content) {
        document.getElementById('saveResult').innerHTML = '<p style="color:red">配方名和内容必填</p>';
        return;
      }

      var body = {
        title: title,
        content: content,
        ingredients: Object.keys(selected),
        base_spirit: document.getElementById('recipeBaseSpirit').value,
        difficulty: document.getElementById('recipeDifficulty').value,
        season: document.getElementById('recipeSeason').value || null,
      };

      fetch('/api/lab/recipes', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(body)
      }).then(function(r){return r.json();}).then(function(d){
        if (action === 'submit' && d.doc_id) {
          return fetch('/api/lab/recipes/' + d.doc_id + '/submit', {method: 'POST'})
            .then(function(){return d;});
        }
        return d;
      }).then(function(d){
        document.getElementById('saveResult').innerHTML =
          '<p style="color:green">保存成功！配方 ID: ' + d.doc_id +
          (action === 'submit' ? '（已提交审核）' : '（草稿）') + '</p>';
      }).catch(function(e){
        document.getElementById('saveResult').innerHTML = '<p style="color:red">保存失败: ' + e.message + '</p>';
      });
    }

    renderIngredientPicker();
  </script>
</body>
</html>
```

- [ ] **Step 3: 导航增加创作入口**

修改 `/workspace/design/mockup/_nav.js`，在 NAV_ITEMS 数组的"实验室"项之后增加：

```javascript
  { href: 'recipe-editor.html', label: '创作', page: 'editor' },
```

- [ ] **Step 4: 验证文件**

Run: `cd /workspace && ls -la design/mockup/recipe-editor.html && grep -c "recipe-editor\|创作" design/mockup/_nav.js`

Expected: 文件存在，_nav.js 有匹配

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add design/mockup/recipe-editor.html design/mockup/_nav.js design/mockup/_components.css && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ugc): 配方编辑器页面

- recipe-editor.html: 配方名/基酒/难度/季节/材料选择/内容编辑
- _nav.js: 导航增加"创作"入口
- _components.css: 编辑器组件样式
- 支持保存草稿 / 保存并提交审核"
```

---

## Task 6: 前端 — doc-detail 变体区 + dashboard 审核队列

**Files:**
- Modify: `design/mockup/doc-detail.html`
- Modify: `design/mockup/dashboard.html`
- Modify: `design/mockup/_components.css`

- [ ] **Step 1: 追加变体区样式**

在 `design/mockup/_components.css` 末尾追加：

```css


/* === M4.3 变体展示 + 审核队列 === */
.variant-section {
  margin: var(--sp-5) 0; padding: var(--sp-4);
  background: var(--ink-50); border-radius: var(--r-md);
}
.variant-section h3 {
  font-family: var(--font-serif); font-size: var(--fs-base);
  color: var(--brand-700); margin-bottom: var(--sp-2);
}
.variant-item {
  display: flex; justify-content: space-between; align-items: center;
  padding: var(--sp-2) 0; border-bottom: 1px solid var(--ink-200);
}
.variant-item:last-child { border-bottom: none; }
.variant-item .v-title { font-weight: 500; }
.variant-item .v-note { font-size: var(--fs-xs); color: var(--ink-400); }

.review-queue {
  background: #fff; border: 1px solid var(--ink-200);
  border-radius: var(--r-lg); padding: var(--sp-5);
  margin: var(--sp-4) 0;
}
.review-queue h3 {
  font-family: var(--font-serif); font-size: var(--fs-lg);
  color: var(--brand-700); margin-bottom: var(--sp-3);
}
.review-item {
  display: flex; justify-content: space-between; align-items: center;
  padding: var(--sp-3) 0; border-bottom: 1px solid var(--ink-200);
}
.review-item:last-child { border-bottom: none; }
.review-item .r-title { font-weight: 500; }
.review-item .r-actions { display: flex; gap: var(--sp-2); }
.review-item .btn-approve {
  background: var(--brand-700); color: #fff; border: none;
  padding: 4px var(--sp-3); border-radius: var(--r-sm);
  font-size: var(--fs-xs); cursor: pointer;
}
.review-item .btn-reject {
  background: var(--ink-200); color: var(--ink-700); border: none;
  padding: 4px var(--sp-3); border-radius: var(--r-sm);
  font-size: var(--fs-xs); cursor: pointer;
}
```

- [ ] **Step 2: doc-detail.html 增加变体区**

先 Read `/workspace/design/mockup/doc-detail.html`，找到配方内容展示区之后的位置。

在配方内容区之后插入变体区：

```html

    <!-- 变体列表（M4.3） -->
    <section class="variant-section" id="variantSection" style="display:none;">
      <h3>变体配方</h3>
      <div id="variantList"></div>
    </section>
```

在 doc-detail.html 的 `<script>` 末尾追加（需获取当前 doc_id，从 URL 参数 chunk 反查或直接用 doc_id 参数）：

```javascript
    // M4.3: 加载变体列表
    var urlParams = new URLSearchParams(window.location.search);
    var chunkId = urlParams.get('chunk');
    // 若有 doc_id 参数直接用，否则需通过 chunk 反查（简化：此处假设有 doc_id）
    var docId = urlParams.get('doc_id');
    if (docId) {
      fetch('/api/lab/recipes/' + docId + '/variants').then(function(r){return r.json();}).then(function(d){
        if (d.items && d.items.length > 0) {
          document.getElementById('variantSection').style.display = 'block';
          document.getElementById('variantList').innerHTML = d.items.map(function(v){
            return '<div class="variant-item">' +
              '<span class="v-title">' + v.variant_title + '</span>' +
              '<span class="v-note">' + (v.variant_note || '') + '</span>' +
              '<a href="doc-detail.html?doc_id=' + v.variant_doc_id + '">查看</a>' +
            '</div>';
          }).join('');
        }
      }).catch(function(){});
    }
```

- [ ] **Step 3: dashboard.html 增加审核队列**

在 dashboard.html 的实验室运营卡片组之后（或同步面板之后）插入审核队列：

```html

    <!-- 待审核配方队列（M4.3） -->
    <section class="review-queue" id="reviewQueue" style="display:none;">
      <h3>待审核配方 <span id="reviewCount" style="font-size:var(--fs-sm);color:var(--ink-400);"></span></h3>
      <div id="reviewList"></div>
    </section>
```

在 dashboard.html 的 `<script>` 末尾追加：

```javascript
    // M4.3: 加载待审核配方
    fetch('/api/lab/recipes?status=pending').then(function(r){return r.json();}).then(function(d){
      if (d.items && d.items.length > 0) {
        document.getElementById('reviewQueue').style.display = 'block';
        document.getElementById('reviewCount').textContent = '(' + d.items.length + ')';
        document.getElementById('reviewList').innerHTML = d.items.map(function(r){
          return '<div class="review-item">' +
            '<span class="r-title">' + r.title + '</span>' +
            '<div class="r-actions">' +
              '<button class="btn-approve" onclick="approveRecipe(\'' + r.doc_id + '\')">通过</button>' +
              '<button class="btn-reject" onclick="rejectRecipe(\'' + r.doc_id + '\')">驳回</button>' +
            '</div>' +
          '</div>';
        }).join('');
      }
    }).catch(function(){});

    function approveRecipe(docId) {
      fetch('/api/lab/recipes/' + docId + '/approve', {method: 'POST'}).then(function(){location.reload();});
    }
    function rejectRecipe(docId) {
      var reason = prompt('驳回理由：');
      if (reason === null) return;
      fetch('/api/lab/recipes/' + docId + '/reject', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({reason: reason})
      }).then(function(){location.reload();});
    }
```

- [ ] **Step 4: 验证文件**

Run: `cd /workspace && grep -c "variant-section\|review-queue" design/mockup/doc-detail.html design/mockup/dashboard.html design/mockup/_components.css`

Expected: 三处有匹配

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add design/mockup/_components.css design/mockup/doc-detail.html design/mockup/dashboard.html && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ugc): doc-detail 变体区 + dashboard 审核队列

- _components.css: 变体展示 + 审核队列样式
- doc-detail.html: 配方详情增加变体列表
- dashboard.html: 增加待审核配方队列（通过/驳回）"
```

---

## Task 7: 全量回归测试 + 推送

- [ ] **Step 1: 运行 M4.3 全量测试**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ugc.py -v`

Expected: 全部 PASS（约 15 个测试）

- [ ] **Step 2: 运行全量回归测试**

Run: `cd /workspace && python -m pytest tests/ -v --tb=short`

Expected: 全部 PASS，无回归（M0-M4.2 不受影响）

- [ ] **Step 3: 推送到远端**

```bash
cd /workspace && git push origin feature/m0-mvp
```

Expected: 推送成功

---

## Self-Review

### 1. Spec 覆盖检查

| Spec 章节 | 覆盖 Task | 状态 |
|---|---|---|
| §4.1 配方编辑器 | Task 5（recipe-editor.html） | ✅ |
| §4.1 材料选择器复用 | Task 5（COMMON_INGREDIENTS + toggleIng） | ✅ |
| §4.1 保存（source=ugc, verified=false） | Task 2（create_recipe）+ Task 4（API） | ✅ |
| §4.2 审核状态机 | Task 2（draft→pending→published/rejected）+ Task 4（API） | ✅ |
| §4.2 审核入口（dashboard 队列） | Task 6（dashboard 审核队列） | ✅ |
| §4.3 变体关联 | Task 1（RecipeVariant 表）+ Task 3（recipe_variants.py）+ Task 4（API） | ✅ |
| §4.3 变体展示 | Task 6（doc-detail 变体区） | ✅ |
| §5 RecipeVariant 模型 | Task 1 | ✅ |
| §6 API（recipes CRUD + submit/approve/reject + variants） | Task 4 | ✅ |
| §7 前端（recipe-editor + doc-detail + dashboard） | Task 5 + Task 6 | ✅ |

### 2. Placeholder 扫描

- ✅ 无 TBD/TODO（recipe-editor 的 COMMON_INGREDIENTS 是合理的简化，注释说明）
- ✅ 所有代码块完整
- ✅ 所有测试用例有具体断言

### 3. 类型一致性检查

| 名称 | 定义处 | 使用处 | 一致 |
|---|---|---|---|
| `RecipeVariant` 字段 | Task 1 | Task 3（recipe_variants.py 使用 base_doc_id/variant_doc_id/variant_note） | ✅ |
| `create_recipe()` 返回 | Task 2 | Task 4（API 响应） | ✅ |
| `create_variant_link()` 签名 | Task 3 | Task 4（API 调用） | ✅ |
| `get_variants()` 返回 | Task 3 | Task 4（API）+ Task 6（前端 JS） | ✅ |
| `status` 值域 | Task 2（draft/pending/published/rejected） | Task 4（API 校验）+ Task 6（筛选） | ✅ |
| `source` 值 "ugc" | Task 2（create_recipe 设置） | Task 4 + Task 5（前端） | ✅ |

---

**Plan complete.** M4.3 UGC 调酒研究室，7 个 Task，TDD 流程，每 Task 独立 commit。

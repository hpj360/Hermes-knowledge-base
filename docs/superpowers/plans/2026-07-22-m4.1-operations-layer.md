# M4.1 自动运营层实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 M3 实验室基础上新增自动运营层：每日推荐（季节+热门+随机）+ 缺材料反馈循环（统计高频缺失→优化替代表）+ 运营看板扩展（dashboard 实验室健康度卡片）。

**Architecture:** 复用 M3 的 recipe_stats / substitutes / ingredients 模块。新增 MissingIngredientStats 表记录材料维度缺失统计。每日推荐用日期 seed 保证同一天稳定。运营看板聚合多个指标一次性返回。前端在 index.html / dashboard.html / lab.html 三处增加入口。

**Tech Stack:** Python 3.11 / FastAPI / SQLModel / SQLite / 原生 HTML+CSS+JS

---

## 文件结构

### 新建文件（后端）
- `src/hermes_kb/daily_recipe.py` — 每日推荐算法（季节权重 + 热门 + 随机）
- `src/hermes_kb/missing_stats.py` — 缺材料统计（计数 + 排行）
- `src/hermes_kb/lab_dashboard.py` — 运营看板指标聚合

### 新建文件（测试）
- `tests/test_kb/test_lab_ops.py` — M4.1 测试（每日推荐 + 缺材料 + 看板）

### 修改文件
- `src/hermes_kb/models.py` — 新增 `MissingIngredientStats` 表
- `src/hermes_kb/seed_recipes.py` — 8 款配方增加 `season` 字段
- `src/hermes_kb/recipe_match.py` — 匹配时记录缺失材料到 MissingIngredientStats
- `src/hermes_kb/app.py` — 新增 4 个端点（daily / missing-stats / substitute / dashboard）
- `design/mockup/index.html` — 首页增加今日推荐卡片
- `design/mockup/dashboard.html` — 增加实验室运营卡片组
- `design/mockup/lab.html` — 匹配区上方增加今日推荐入口
- `design/mockup/_components.css` — 追加运营组件样式

---

## Task 1: 数据模型 — MissingIngredientStats 表

**Files:**
- Modify: `src/hermes_kb/models.py`（末尾追加）

- [ ] **Step 1: 写失败测试**

创建 `tests/test_kb/test_lab_ops.py`：

```python
"""M4.1 自动运营层测试：每日推荐 + 缺材料统计 + 运营看板。"""
from __future__ import annotations

import pytest
from sqlmodel import select


def test_missing_ingredient_stats_model(tmp_db):
    """MissingIngredientStats 表可创建并写入。"""
    from hermes_kb.models import MissingIngredientStats
    from hermes_kb.database import get_session

    with get_session() as session:
        stat = MissingIngredientStats(
            canonical="君度", missing_count=5
        )
        session.add(stat)
        session.commit()
        session.refresh(stat)
        assert stat.canonical == "君度"
        assert stat.missing_count == 5
        assert stat.last_missing_at is None
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_missing_ingredient_stats_model -v`

Expected: FAIL with "cannot import name 'MissingIngredientStats'"

- [ ] **Step 3: 实现数据模型**

在 `src/hermes_kb/models.py` 末尾（`IngredientSubstitute` 之后）追加：

```python


class MissingIngredientStats(SQLModel, table=True):
    """M4.1：缺失材料统计（材料维度，反向优化替代表）。"""

    canonical: str = Field(primary_key=True, max_length=64)
    missing_count: int = Field(default=0)
    last_missing_at: datetime | None = Field(default=None)
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_missing_ingredient_stats_model -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/models.py tests/test_kb/test_lab_ops.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): MissingIngredientStats 数据模型

- 材料维度的缺失统计（canonical/missing_count/last_missing_at）
- 支撑 M4.1 缺材料反馈循环"
```

---

## Task 2: seed_recipes season 标签扩展

**Files:**
- Modify: `src/hermes_kb/seed_recipes.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ops.py` 追加：

```python


def test_seed_recipes_have_season():
    """每款种子配方都有 season 标签。"""
    from hermes_kb.seed_recipes import SEED_RECIPES

    valid_seasons = {"spring", "summer", "autumn", "winter"}
    for recipe in SEED_RECIPES:
        assert "season" in recipe, f"配方 {recipe['title']} 缺 season 字段"
        assert recipe["season"] in valid_seasons, (
            f"配方 {recipe['title']} 的 season={recipe['season']} 不合法"
        )


def test_seed_recipes_season_distribution():
    """季节标签至少覆盖 3 个季节（避免全挤一季）。"""
    from hermes_kb.seed_recipes import SEED_RECIPES

    seasons = {r["season"] for r in SEED_RECIPES}
    assert len(seasons) >= 3, f"季节覆盖不足: {seasons}"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_seed_recipes_have_season tests/test_kb/test_lab_ops.py::test_seed_recipes_season_distribution -v`

Expected: FAIL with "缺 season 字段"

- [ ] **Step 3: 为 8 款配方添加 season**

修改 `src/hermes_kb/seed_recipes.py`，为每个配方 dict 增加 `"season"` 字段。季节映射：

| 配方 | season | 理由 |
|---|---|---|
| 马天尼 Martini | autumn | 干冽醇厚，秋日经典 |
| 莫吉托 Mojito | summer | 清爽冰凉，夏日长饮 |
| 尼格罗尼 Negroni | autumn | 苦甜饱满，秋日药草香 |
| 玛格丽特 Margarita | summer | 酸甜咸冰爽，夏日墨西哥 |
| 古典鸡尾酒 Old Fashioned | winter | 醇厚烈口，冬日威士忌 |
| 白色佳人 White Lady | spring | 酸香优雅，春日花香 |
| 龙舌兰日出 Tequila Sunrise | summer | 果香甜美，夏日渐层 |
| 血腥玛丽 Bloody Mary | winter | 咸鲜辛辣，冬日宿醉救星 |

逐个修改每个配方 dict，在 `"difficulty"` 行之后增加 `"season"` 行。例如马天尼：

```python
    {
        "title": "马天尼 Martini",
        "base_spirit": "gin",
        "difficulty": "easy",
        "season": "autumn",
        "ingredients": ["金酒", "味美思", "橄榄"],
        "content": """# 马天尼 Martini
```

其余 7 款同样在 `"difficulty"` 后插入 `"season"` 行。

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_seed_recipes_have_season tests/test_kb/test_lab_ops.py::test_seed_recipes_season_distribution -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/seed_recipes.py tests/test_kb/test_lab_ops.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): seed_recipes 增加 season 标签

- 8 款 IBA 配方补齐 season（spring/summer/autumn/winter）
- 覆盖 4 季节，支撑每日推荐季节权重算法"
```

---

## Task 3: 每日推荐算法 — daily_recipe.py

**Files:**
- Create: `src/hermes_kb/daily_recipe.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ops.py` 追加：

```python


@pytest.fixture
def seeded_recipes_ops(tmp_db):
    """导入种子配方（含 season）。"""
    from hermes_kb.rag import ImportService
    from hermes_kb.seed_recipes import SEED_RECIPES
    from hermes_kb.database import get_session
    from hermes_kb.models import Document
    from sqlmodel import select

    importer = ImportService()
    for recipe in SEED_RECIPES:
        importer.import_text(
            content=recipe["content"],
            title=recipe["title"],
            source_type="seed",
            file_type="md",
        )
        with get_session() as session:
            doc = session.exec(
                select(Document).where(Document.title == recipe["title"])
            ).first()
            if doc:
                doc.category = "recipe"
                session.add(doc)
                session.commit()
    return importer


def test_daily_recipe_returns_one(seeded_recipes_ops):
    """每日推荐返回一款配方。"""
    from hermes_kb.daily_recipe import daily_recipe

    result = daily_recipe()
    assert result is not None
    assert "title" in result
    assert "doc_id" in result
    assert "reason" in result
    assert result["reason"] in ["season", "hot", "random"]


def test_daily_recipe_stable_per_day(seeded_recipes_ops):
    """同一天多次调用返回同一款。"""
    from hermes_kb.daily_recipe import daily_recipe

    r1 = daily_recipe()
    r2 = daily_recipe()
    assert r1["doc_id"] == r2["doc_id"]


def test_daily_recipe_reason_format(seeded_recipes_ops):
    """reason 字段格式正确。"""
    from hermes_kb.daily_recipe import daily_recipe

    result = daily_recipe()
    assert isinstance(result["reason"], str)
    assert len(result["reason"]) > 0
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_daily_recipe_returns_one tests/test_kb/test_lab_ops.py::test_daily_recipe_stable_per_day tests/test_kb/test_lab_ops.py::test_daily_recipe_reason_format -v`

Expected: FAIL with "No module named 'hermes_kb.daily_recipe'"

- [ ] **Step 3: 实现每日推荐算法**

创建 `src/hermes_kb/daily_recipe.py`：

```python
"""每日推荐算法（M4.1）。

权重：季节 60% + 热门 30% + 随机 10%
稳定性：同一天返回同一款（用日期作随机种子）
"""
from __future__ import annotations

import random
from datetime import date, datetime, timezone
from typing import Any

from sqlmodel import select

from hermes_kb.database import get_session
from hermes_kb.models import Chunk, Document
from hermes_kb.recipe_stats import get_hot_recipes
from hermes_kb.seed_recipes import SEED_RECIPES


def _current_season() -> str:
    """根据当前月份返回季节。"""
    month = date.today().month
    if month in (3, 4, 5):
        return "spring"
    if month in (6, 7, 8):
        return "summer"
    if month in (9, 10, 11):
        return "autumn"
    return "winter"


def _seasonal_pool(season: str) -> list[dict[str, Any]]:
    """获取某季节的配方池（从种子数据 + 知识库）。"""
    # 种子元信息：title → season
    seed_season: dict[str, str] = {r["title"]: r.get("season", "") for r in SEED_RECIPES}

    recipes: list[dict[str, Any]] = []
    with get_session() as session:
        docs = session.exec(
            select(Document).where(Document.category == "recipe")
        ).all()
        for doc in docs:
            doc_season = seed_season.get(doc.title, "")
            if doc_season == season:
                first_chunk = session.exec(
                    select(Chunk)
                    .where(Chunk.doc_id == doc.doc_id)
                    .order_by(Chunk.idx)
                ).first()
                meta = next((r for r in SEED_RECIPES if r["title"] == doc.title), {})
                recipes.append(
                    {
                        "title": doc.title,
                        "doc_id": doc.doc_id,
                        "chunk_rowid": first_chunk.id if first_chunk else None,
                        "base_spirit": meta.get("base_spirit", ""),
                        "difficulty": meta.get("difficulty", ""),
                    }
                )
    return recipes


def daily_recipe() -> dict[str, Any] | None:
    """每日推荐：季节 60% + 热门 30% + 随机 10%。

    Returns:
        {title, doc_id, chunk_rowid, reason, base_spirit, difficulty}
        reason: "season" | "hot" | "random"
        若知识库无配方返回 None。
    """
    today_seed = int(date.today().toordinal())
    rng = random.Random(today_seed)

    roll = rng.random()
    season = _current_season()

    # 60% 季节池
    if roll < 0.6:
        pool = _seasonal_pool(season)
        if pool:
            choice = rng.choice(pool)
            choice["reason"] = "season"
            return choice

    # 30% 热门池
    if roll < 0.9:
        hot = get_hot_recipes(limit=10, days=30)
        if hot:
            choice = rng.choice(hot)
            return {
                "title": choice["title"],
                "doc_id": choice["doc_id"],
                "chunk_rowid": choice.get("chunk_rowid"),
                "reason": "hot",
                "base_spirit": "",
                "difficulty": "",
            }

    # 10% 全库随机
    with get_session() as session:
        docs = session.exec(
            select(Document).where(Document.category == "recipe")
        ).all()
        if not docs:
            return None
        doc = rng.choice(docs)
        first_chunk = session.exec(
            select(Chunk)
            .where(Chunk.doc_id == doc.doc_id)
            .order_by(Chunk.idx)
        ).first()
        meta = next((r for r in SEED_RECIPES if r["title"] == doc.title), {})
        return {
            "title": doc.title,
            "doc_id": doc.doc_id,
            "chunk_rowid": first_chunk.id if first_chunk else None,
            "reason": "random",
            "base_spirit": meta.get("base_spirit", ""),
            "difficulty": meta.get("difficulty", ""),
        }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_daily_recipe_returns_one tests/test_kb/test_lab_ops.py::test_daily_recipe_stable_per_day tests/test_kb/test_lab_ops.py::test_daily_recipe_reason_format -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/daily_recipe.py tests/test_kb/test_lab_ops.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): 每日推荐算法

- daily_recipe(): 季节 60% + 热门 30% + 随机 10%
- 日期 seed 保证同一天稳定
- reason 字段说明推荐理由（season/hot/random）"
```

---

## Task 4: 缺材料统计 — missing_stats.py

**Files:**
- Create: `src/hermes_kb/missing_stats.py`
- Modify: `src/hermes_kb/recipe_match.py`（匹配时记录缺失）

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ops.py` 追加：

```python


def test_missing_stats_increment(seeded_recipes_ops):
    """记录缺失材料计数。"""
    from hermes_kb.missing_stats import increment_missing, get_missing_stats

    increment_missing("君度")
    increment_missing("君度")
    increment_missing("金巴利")

    stat = get_missing_stats("君度")
    assert stat is not None
    assert stat["missing_count"] == 2
    assert stat["last_missing_at"] is not None


def test_missing_stats_top(seeded_recipes_ops):
    """缺失材料排行。"""
    from hermes_kb.missing_stats import increment_missing, get_top_missing

    for _ in range(5):
        increment_missing("君度")
    for _ in range(3):
        increment_missing("金巴利")
    for _ in range(1):
        increment_missing("苦精")

    top = get_top_missing(limit=3)
    assert len(top) == 3
    assert top[0]["canonical"] == "君度"
    assert top[0]["missing_count"] == 5
    assert top[1]["canonical"] == "金巴利"
    assert top[2]["canonical"] == "苦精"


def test_match_records_missing(seeded_recipes_ops):
    """match_recipes 调用后缺失材料被统计。"""
    from hermes_kb.recipe_match import match_recipes
    from hermes_kb.missing_stats import get_missing_stats

    # 白色佳人需要金酒+君度+柠檬汁，只给金酒+柠檬汁 → 缺君度
    match_recipes({"金酒", "柠檬汁"})
    stat = get_missing_stats("君度")
    assert stat is not None
    assert stat["missing_count"] >= 1
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_missing_stats_increment tests/test_kb/test_lab_ops.py::test_missing_stats_top tests/test_kb/test_lab_ops.py::test_match_records_missing -v`

Expected: FAIL with "No module named 'hermes_kb.missing_stats'"

- [ ] **Step 3: 实现缺材料统计模块**

创建 `src/hermes_kb/missing_stats.py`：

```python
"""缺材料统计（M4.1 反馈循环）。

统计时机：match_recipes 返回 partial_match 时，对每个 missing 材料计数 +1。
用途：反向优化替代关系表，高频缺失材料提示运营补充替代。
"""
from __future__ import annotations

from datetime import datetime, timezone
from typing import Any

from sqlmodel import select

from hermes_kb.database import get_session
from hermes_kb.models import MissingIngredientStats


def increment_missing(canonical: str) -> None:
    """缺失材料计数 +1。"""
    with get_session() as session:
        stat = session.get(MissingIngredientStats, canonical)
        now = datetime.now(timezone.utc)
        if stat:
            stat.missing_count += 1
            stat.last_missing_at = now
        else:
            stat = MissingIngredientStats(
                canonical=canonical, missing_count=1, last_missing_at=now
            )
        session.add(stat)
        session.commit()


def get_missing_stats(canonical: str) -> dict[str, Any] | None:
    """查询单个材料的缺失统计。"""
    with get_session() as session:
        stat = session.get(MissingIngredientStats, canonical)
        if not stat:
            return None
        return {
            "canonical": stat.canonical,
            "missing_count": stat.missing_count,
            "last_missing_at": stat.last_missing_at.isoformat()
            if stat.last_missing_at
            else None,
        }


def get_top_missing(limit: int = 10) -> list[dict[str, Any]]:
    """缺失材料排行（按 missing_count 降序）。"""
    with get_session() as session:
        rows = session.exec(
            select(MissingIngredientStats)
            .where(MissingIngredientStats.missing_count > 0)
            .order_by(MissingIngredientStats.missing_count.desc())
            .limit(limit)
        ).all()
        return [
            {
                "canonical": r.canonical,
                "missing_count": r.missing_count,
                "last_missing_at": r.last_missing_at.isoformat()
                if r.last_missing_at
                else None,
            }
            for r in rows
        ]
```

- [ ] **Step 4: 修改 recipe_match.py 记录缺失**

在 `src/hermes_kb/recipe_match.py` 的 `match_recipes` 函数中，`partial_match.append(base)` 之前增加缺失统计调用。

找到（约 151-153 行）：

```python
        elif len(truly_missing) <= 2:
            base["missing"] = truly_missing
            base["missing_count"] = len(truly_missing)
            partial_match.append(base)
```

替换为：

```python
        elif len(truly_missing) <= 2:
            base["missing"] = truly_missing
            base["missing_count"] = len(truly_missing)
            # M4.1：记录缺失材料统计（反馈循环）
            from hermes_kb.missing_stats import increment_missing

            for m in truly_missing:
                try:
                    increment_missing(m)
                except Exception:
                    pass  # 统计失败不影响主流程
            partial_match.append(base)
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_missing_stats_increment tests/test_kb/test_lab_ops.py::test_missing_stats_top tests/test_kb/test_lab_ops.py::test_match_records_missing -v`

Expected: PASS

- [ ] **Step 6: 提交**

```bash
cd /workspace && git add src/hermes_kb/missing_stats.py src/hermes_kb/recipe_match.py tests/test_kb/test_lab_ops.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): 缺材料反馈循环

- missing_stats.py: increment_missing/get_missing_stats/get_top_missing
- recipe_match.py: partial_match 时记录缺失材料计数
- 支撑运营优化替代关系表"
```

---

## Task 5: 运营看板聚合 — lab_dashboard.py

**Files:**
- Create: `src/hermes_kb/lab_dashboard.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ops.py` 追加：

```python


def test_lab_dashboard_aggregation(seeded_recipes_ops):
    """运营看板返回完整指标。"""
    from hermes_kb.lab_dashboard import get_lab_dashboard
    from hermes_kb.recipe_match import match_recipes

    # 制造一些匹配和缺失数据
    match_recipes({"金酒", "味美思", "橄榄"})
    match_recipes({"金酒", "柠檬汁"})

    dashboard = get_lab_dashboard()
    assert "recipe_count" in dashboard
    assert "weekly_match_count" in dashboard
    assert "top_recipe" in dashboard
    assert "top_missing" in dashboard
    assert "substitute_coverage" in dashboard
    assert "user_substitute_count" in dashboard
    assert "daily_recipe" in dashboard
    assert "season_coverage" in dashboard

    assert dashboard["recipe_count"] >= 8
    assert isinstance(dashboard["substitute_coverage"], (int, float))
    assert 0 <= dashboard["substitute_coverage"] <= 1
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_lab_dashboard_aggregation -v`

Expected: FAIL with "No module named 'hermes_kb.lab_dashboard'"

- [ ] **Step 3: 实现运营看板聚合**

创建 `src/hermes_kb/lab_dashboard.py`：

```python
"""运营看板指标聚合（M4.1）。

一次性返回实验室运营所需的全部指标。
"""
from __future__ import annotations

from datetime import datetime, timedelta, timezone
from typing import Any

from sqlmodel import func, select

from hermes_kb.database import get_session
from hermes_kb.daily_recipe import daily_recipe
from hermes_kb.ingredients import all_canonical
from hermes_kb.missing_stats import get_top_missing
from hermes_kb.models import Document, IngredientSubstitute, RecipeStats
from hermes_kb.recipe_stats import get_hot_recipes
from hermes_kb.seed_recipes import SEED_RECIPES
from hermes_kb.substitutes import SUBSTITUTES_PRESET, list_all_substitutes


def get_lab_dashboard() -> dict[str, Any]:
    """聚合实验室运营指标。"""
    # 配方总数
    with get_session() as session:
        recipe_count = session.exec(
            select(func.count(Document.doc_id)).where(
                Document.category == "recipe"
            )
        ).one()

        # 本周匹配数（近 7 天）
        cutoff = datetime.now(timezone.utc) - timedelta(days=7)
        weekly_match = session.exec(
            select(func.sum(RecipeStats.match_count)).where(
                RecipeStats.last_matched_at >= cutoff
            )
        ).one() or 0

        # 用户自定义替代数
        user_sub_count = session.exec(
            select(func.count(IngredientSubstitute.id)).where(
                IngredientSubstitute.source == "user"
            )
        ).one()

    # Top 配方
    hot = get_hot_recipes(limit=1, days=30)
    top_recipe = hot[0]["title"] if hot else None

    # 高频缺失
    top_missing_list = get_top_missing(limit=1)
    top_missing = top_missing_list[0] if top_missing_list else None

    # 替代表覆盖率
    all_ings = set(all_canonical())
    covered = set(SUBSTITUTES_PRESET.keys())
    with get_session() as session:
        rows = session.exec(
            select(IngredientSubstitute.canonical).distinct()
        ).all()
        covered.update(rows)
    substitute_coverage = len(covered & all_ings) / len(all_ings) if all_ings else 0

    # 今日推荐
    daily = daily_recipe()

    # 季节标签覆盖
    seasonal_recipes = sum(1 for r in SEED_RECIPES if r.get("season"))
    season_coverage = seasonal_recipes / len(SEED_RECIPES) if SEED_RECIPES else 0

    return {
        "recipe_count": recipe_count,
        "weekly_match_count": weekly_match,
        "top_recipe": top_recipe,
        "top_missing": top_missing,
        "substitute_coverage": round(substitute_coverage, 2),
        "user_substitute_count": user_sub_count,
        "daily_recipe": daily["title"] if daily else None,
        "season_coverage": round(season_coverage, 2),
    }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_lab_dashboard_aggregation -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/lab_dashboard.py tests/test_kb/test_lab_ops.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): 运营看板指标聚合

- get_lab_dashboard(): 8 项指标一次性返回
- recipe_count/weekly_match/top_recipe/top_missing
- substitute_coverage/user_substitute_count/daily_recipe/season_coverage"
```

---

## Task 6: API 端点 — daily + missing-stats + substitute + dashboard

**Files:**
- Modify: `src/hermes_kb/app.py`（在 M3 实验室端点之后追加）

- [ ] **Step 1: 写失败测试**

在 `tests/test_kb/test_lab_ops.py` 追加：

```python


def test_api_lab_daily(seeded_recipes_ops, client):
    """GET /api/lab/daily 返回每日推荐。"""
    resp = client.get("/api/lab/daily")
    assert resp.status_code == 200
    data = resp.json()
    assert "title" in data
    assert "doc_id" in data
    assert data["reason"] in ["season", "hot", "random"]


def test_api_lab_missing_stats(seeded_recipes_ops, client):
    """GET /api/lab/missing-stats 返回缺失排行。"""
    # 先制造缺失数据
    client.get("/api/lab/match", params={"ingredients": "金酒,柠檬汁"})

    resp = client.get("/api/lab/missing-stats", params={"limit": 5})
    assert resp.status_code == 200
    data = resp.json()
    assert "items" in data
    assert len(data["items"]) > 0
    assert "canonical" in data["items"][0]
    assert "missing_count" in data["items"][0]


def test_api_lab_substitute_save(seeded_recipes_ops, client):
    """POST /api/lab/substitute 保存用户自定义替代。"""
    resp = client.post(
        "/api/lab/substitute",
        json={"canonical": "君度", "substitute": "自制橙皮酒"},
    )
    assert resp.status_code == 200
    assert resp.json()["status"] == "ok"

    from hermes_kb.substitutes import get_substitutes
    assert "自制橙皮酒" in get_substitutes("君度")


def test_api_lab_dashboard(seeded_recipes_ops, client):
    """GET /api/lab/dashboard 返回运营看板。"""
    resp = client.get("/api/lab/dashboard")
    assert resp.status_code == 200
    data = resp.json()
    assert data["recipe_count"] >= 8
    assert "substitute_coverage" in data
    assert "daily_recipe" in data
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_api_lab_daily tests/test_kb/test_lab_ops.py::test_api_lab_missing_stats tests/test_kb/test_lab_ops.py::test_api_lab_substitute_save tests/test_kb/test_lab_ops.py::test_api_lab_dashboard -v`

Expected: FAIL with "404 Not Found"

- [ ] **Step 3: 实现 API 端点**

在 `src/hermes_kb/app.py` 的 M3 实验室端点之后（`@app.post("/api/lab/view/{doc_id}")` 之后）、静态文件挂载之前，追加：

```python

    # -----------------------------------------------------------------------
    # M4.1：实验室自动运营层
    # -----------------------------------------------------------------------
    @app.get("/api/lab/daily")
    async def lab_daily() -> dict[str, Any]:
        """每日推荐配方。"""
        from hermes_kb.daily_recipe import daily_recipe

        result = daily_recipe()
        return result or {"title": None, "reason": "empty"}

    @app.get("/api/lab/missing-stats")
    async def lab_missing_stats(limit: int = 10) -> dict[str, Any]:
        """缺失材料排行。"""
        from hermes_kb.missing_stats import get_top_missing

        limit = max(1, min(limit, 50))
        return {"items": get_top_missing(limit=limit)}

    @app.post("/api/lab/substitute")
    async def lab_save_substitute(req: dict[str, Any]) -> dict[str, Any]:
        """保存用户自定义替代关系。"""
        from hermes_kb.substitutes import add_user_substitute

        canonical = (req.get("canonical") or "").strip()
        substitute = (req.get("substitute") or "").strip()
        if not canonical or not substitute:
            raise HTTPException(status_code=400, detail="canonical 和 substitute 必填")
        add_user_substitute(canonical, substitute)
        return {"canonical": canonical, "substitute": substitute, "status": "ok"}

    @app.get("/api/lab/dashboard")
    async def lab_dashboard_endpoint() -> dict[str, Any]:
        """实验室运营看板聚合指标。"""
        from hermes_kb.lab_dashboard import get_lab_dashboard

        return get_lab_dashboard()
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py::test_api_lab_daily tests/test_kb/test_lab_ops.py::test_api_lab_missing_stats tests/test_kb/test_lab_ops.py::test_api_lab_substitute_save tests/test_kb/test_lab_ops.py::test_api_lab_dashboard -v`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hermes_kb/app.py tests/test_kb/test_lab_ops.py && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): API 端点 daily + missing-stats + substitute + dashboard

- GET /api/lab/daily: 每日推荐
- GET /api/lab/missing-stats: 缺失材料排行
- POST /api/lab/substitute: 保存用户自定义替代
- GET /api/lab/dashboard: 运营看板聚合"
```

---

## Task 7: 前端 — index.html 今日推荐 + lab.html 推荐入口

**Files:**
- Modify: `design/mockup/index.html`
- Modify: `design/mockup/lab.html`
- Modify: `design/mockup/_components.css`

- [ ] **Step 1: 追加组件样式**

在 `design/mockup/_components.css` 末尾追加：

```css


/* === M4.1 运营组件 === */
.daily-recipe {
  background: linear-gradient(135deg, var(--brand-50) 0%, var(--gold-100) 100%);
  border: 1px solid var(--gold-500);
  border-radius: var(--r-lg);
  padding: var(--sp-5) var(--sp-6);
  margin: var(--sp-4) 0;
  display: flex;
  align-items: center;
  gap: var(--sp-4);
  text-decoration: none;
  color: var(--ink-900);
  transition: transform var(--duration-fast) var(--ease-out);
}
.daily-recipe:hover { transform: translateY(-2px); box-shadow: var(--shadow-md); }
.daily-recipe .daily-badge {
  background: var(--gold-500); color: var(--ink-900);
  font-size: var(--fs-xs); padding: 2px var(--sp-2);
  border-radius: var(--r-full); white-space: nowrap;
}
.daily-recipe .daily-name { font-family: var(--font-serif); font-size: var(--fs-lg); flex: 1; }
.daily-recipe .daily-reason { font-size: var(--fs-xs); color: var(--ink-400); }
```

- [ ] **Step 2: index.html 增加今日推荐区**

先读取 `design/mockup/index.html` 找到热门配方区的位置。

Run: `cd /workspace && grep -n "hot-recipes\|本周热门" design/mockup/index.html`

在热门配方区 `<section class="hot-recipes">` 之前插入今日推荐区：

```html

    <!-- 今日推荐（M4.1） -->
    <section id="dailyRecipeSection" style="display:none;">
      <a id="dailyRecipeLink" href="#" class="daily-recipe">
        <span class="daily-badge">今日推荐</span>
        <span class="daily-name" id="dailyRecipeName"></span>
        <span class="daily-reason" id="dailyRecipeReason"></span>
      </a>
    </section>
```

并在 index.html 的 `<script>` 末尾（或 `</body>` 前）追加加载脚本：

```html
  <script>
    // M4.1: 加载今日推荐
    fetch('/api/lab/daily').then(function(r){return r.json();}).then(function(d){
      if (d && d.title) {
        document.getElementById('dailyRecipeSection').style.display = 'block';
        document.getElementById('dailyRecipeName').textContent = d.title;
        var reasonMap = {season:'应季推荐', hot:'本周热门', random:'随机发现'};
        document.getElementById('dailyRecipeReason').textContent = reasonMap[d.reason] || d.reason;
        document.getElementById('dailyRecipeLink').href = 'doc-detail.html?chunk=' + (d.chunk_rowid || '');
      }
    }).catch(function(){});
  </script>
```

- [ ] **Step 3: lab.html 增加今日推荐入口**

在 `design/mockup/lab.html` 的 `.lab-hero` 之后、`.material-selector` 之前插入：

```html

    <!-- 今日推荐（M4.1） -->
    <div id="labDaily" style="display:none; margin-bottom: var(--sp-4);">
      <a id="labDailyLink" href="#" class="daily-recipe" style="margin:0;">
        <span class="daily-badge">今日推荐</span>
        <span class="daily-name" id="labDailyName"></span>
        <span class="daily-reason" id="labDailyReason"></span>
      </a>
    </div>
```

并在 lab.html 的 `<script>` 开头（`var selected = {};` 之前）追加：

```javascript
    // M4.1: 加载今日推荐
    fetch(API_BASE + '/api/lab/daily').then(function(r){return r.json();}).then(function(d){
      if (d && d.title) {
        document.getElementById('labDaily').style.display = 'block';
        document.getElementById('labDailyName').textContent = d.title;
        var reasonMap = {season:'应季推荐', hot:'本周热门', random:'随机发现'};
        document.getElementById('labDailyReason').textContent = reasonMap[d.reason] || d.reason;
        document.getElementById('labDailyLink').href = 'doc-detail.html?chunk=' + (d.chunk_rowid || '');
      }
    }).catch(function(){});
```

- [ ] **Step 4: 验证文件修改**

Run: `cd /workspace && grep -c "daily-recipe" design/mockup/index.html design/mockup/lab.html design/mockup/_components.css`

Expected: 三处都含 daily-recipe 样式/元素

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add design/mockup/_components.css design/mockup/index.html design/mockup/lab.html && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): 前端今日推荐入口

- _components.css: daily-recipe 渐变卡片样式
- index.html: 热门区上方增加今日推荐，JS 自动加载 /api/lab/daily
- lab.html: hero 下方增加今日推荐入口"
```

---

## Task 8: 前端 — dashboard.html 实验室运营卡片组

**Files:**
- Modify: `design/mockup/dashboard.html`
- Modify: `design/mockup/_components.css`

- [ ] **Step 1: 追加看板样式**

在 `design/mockup/_components.css` 末尾追加：

```css


/* === M4.1 运营看板 === */
.lab-dashboard {
  background: #fff; border: 1px solid var(--ink-200);
  border-radius: var(--r-lg); padding: var(--sp-6);
  box-shadow: var(--shadow-sm); margin: var(--sp-6) 0;
}
.lab-dashboard h3 {
  font-family: var(--font-serif); font-size: var(--fs-lg);
  color: var(--brand-700); margin-bottom: var(--sp-4);
  display: flex; align-items: center; gap: var(--sp-2);
}
.lab-dashboard h3 .lab-icon { color: var(--gold-500); }
.lab-metrics {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  gap: var(--sp-3);
}
.lab-metric {
  background: var(--ink-50); border-radius: var(--r-md);
  padding: var(--sp-3); text-align: center;
}
.lab-metric .lab-label { font-size: var(--fs-xs); color: var(--ink-400); margin-bottom: var(--sp-1); }
.lab-metric .lab-num { font-family: var(--font-serif); font-size: var(--fs-xl); color: var(--ink-900); }
.lab-metric .lab-sub { font-size: var(--fs-xs); color: var(--brand-700); margin-top: var(--sp-1); }
.lab-metric.alert .lab-num { color: var(--gold-700); }
```

- [ ] **Step 2: dashboard.html 增加实验室运营卡片**

先读取 `design/mockup/dashboard.html` 找到飞轮健康度区的结束位置。

Run: `cd /workspace && grep -n "flywheel\|审计日志\|section-title" design/mockup/dashboard.html | head -10`

在飞轮健康度区之后（`</section>` 结束之后）插入实验室运营卡片组：

```html

    <!-- 实验室运营（M4.1） -->
    <section class="lab-dashboard" id="labDashboard" style="display:none;">
      <h3><span class="lab-icon">◆</span> 实验室运营</h3>
      <div class="lab-metrics" id="labMetrics">
        <div class="lab-metric">
          <div class="lab-label">配方总数</div>
          <div class="lab-num" id="labRecipeCount">-</div>
        </div>
        <div class="lab-metric">
          <div class="lab-label">本周匹配</div>
          <div class="lab-num" id="labWeeklyMatch">-</div>
          <div class="lab-sub" id="labTopRecipe"></div>
        </div>
        <div class="lab-metric alert">
          <div class="lab-label">高频缺失</div>
          <div class="lab-num" id="labTopMissing">-</div>
          <div class="lab-sub" id="labTopMissingCount"></div>
        </div>
        <div class="lab-metric">
          <div class="lab-label">替代表覆盖</div>
          <div class="lab-num" id="labSubCoverage">-</div>
          <div class="lab-sub" id="labUserSub"></div>
        </div>
        <div class="lab-metric">
          <div class="lab-label">今日推荐</div>
          <div class="lab-num" id="labDailyRecipe" style="font-size:var(--fs-base);">-</div>
          <div class="lab-sub" id="labSeasonCoverage"></div>
        </div>
      </div>
    </section>
```

并在 dashboard.html 的 `<script>` 末尾追加加载脚本：

```html
  <script>
    // M4.1: 加载实验室运营看板
    fetch('/api/lab/dashboard').then(function(r){return r.json();}).then(function(d){
      document.getElementById('labDashboard').style.display = 'block';
      document.getElementById('labRecipeCount').textContent = d.recipe_count || 0;
      document.getElementById('labWeeklyMatch').textContent = d.weekly_match_count || 0;
      document.getElementById('labTopRecipe').textContent = d.top_recipe ? 'Top: ' + d.top_recipe : '';
      if (d.top_missing) {
        document.getElementById('labTopMissing').textContent = d.top_missing.canonical;
        document.getElementById('labTopMissingCount').textContent = '缺 ' + d.top_missing.missing_count + ' 次';
      } else {
        document.getElementById('labTopMissing').textContent = '无';
      }
      document.getElementById('labSubCoverage').textContent = Math.round(d.substitute_coverage * 100) + '%';
      document.getElementById('labUserSub').textContent = '用户自定义 ' + d.user_substitute_count + ' 条';
      document.getElementById('labDailyRecipe').textContent = d.daily_recipe || '-';
      document.getElementById('labSeasonCoverage').textContent = '季节覆盖 ' + Math.round(d.season_coverage * 100) + '%';
    }).catch(function(){});
  </script>
```

- [ ] **Step 3: 验证文件修改**

Run: `cd /workspace && grep -c "lab-dashboard\|labMetrics" design/mockup/dashboard.html && grep -c "lab-dashboard" design/mockup/_components.css`

Expected: dashboard.html 有匹配，_components.css 有 1

- [ ] **Step 4: 提交**

```bash
cd /workspace && git add design/mockup/_components.css design/mockup/dashboard.html && git -c user.name=trae-agent -c user.email=agent@trae.local commit -m "feat(lab-ops): dashboard 实验室运营卡片组

- _components.css: lab-dashboard 卡片组样式（5 指标网格）
- dashboard.html: 飞轮健康度后增加实验室运营区
- JS 自动加载 /api/lab/dashboard 填充 5 项指标"
```

---

## Task 9: 全量回归测试 + 推送

**Files:**
- 无新建，仅运行测试

- [ ] **Step 1: 运行 M4.1 全量测试**

Run: `cd /workspace && python -m pytest tests/test_kb/test_lab_ops.py -v`

Expected: 全部 PASS（约 12 个测试用例）

- [ ] **Step 2: 运行全量回归测试**

Run: `cd /workspace && python -m pytest tests/ -v --tb=short`

Expected: 全部 PASS，无回归（M0/M1/M2/M3 测试不受影响）

- [ ] **Step 3: 推送到远端**

```bash
cd /workspace && git push origin feature/m0-mvp
```

Expected: 推送成功

---

## Self-Review

### 1. Spec 覆盖检查

| Spec 章节 | 覆盖 Task | 状态 |
|---|---|---|
| §2.1 每日推荐算法 | Task 3（daily_recipe.py）+ Task 6（API）+ Task 7（前端入口） | ✅ |
| §2.1 季节标签 | Task 2（seed_recipes season） | ✅ |
| §2.1 日期稳定性 | Task 3（date.today().toordinal() seed） | ✅ |
| §2.2 缺材料反馈循环 | Task 1（MissingIngredientStats 表）+ Task 4（missing_stats.py + recipe_match 记录）+ Task 6（API） | ✅ |
| §2.3 运营看板扩展 | Task 5（lab_dashboard.py）+ Task 6（API）+ Task 8（dashboard.html） | ✅ |
| §5 MissingIngredientStats 模型 | Task 1 | ✅ |
| §6 API 端点（daily/missing-stats/substitute/dashboard） | Task 6 | ✅ |
| §7 前端（index/dashboard/lab 三处） | Task 7 + Task 8 | ✅ |

**缺口**：§2.2 "missing_count > 50 且无预置替代时告警"——M4.1 仅统计，告警逻辑属 M4.2 运营看板增强，正确排除。§2.2 "用户点替代 chip 提示保存为自定义"——需前端交互增强，M4.1 已提供 POST /api/lab/substitute 端点，前端提示可后续迭代。

### 2. Placeholder 扫描

- ✅ 无 TBD/TODO
- ✅ 所有代码块完整
- ✅ 所有测试用例有具体断言
- ✅ 所有 git commit 命令含具体 message

### 3. 类型一致性检查

| 名称 | Task 定义处 | 后续使用 | 一致 |
|---|---|---|---|
| `MissingIngredientStats` 字段 | Task 1 | Task 4（missing_stats.py 使用 canonical/missing_count/last_missing_at） | ✅ |
| `daily_recipe()` 返回字段 | Task 3 | Task 6（API 响应）+ Task 5（lab_dashboard 调用） | ✅ |
| `increment_missing(canonical)` 签名 | Task 4 | Task 4（recipe_match.py 调用） | ✅ |
| `get_top_missing(limit)` 签名 | Task 4 | Task 5（lab_dashboard 调用）+ Task 6（API） | ✅ |
| `get_lab_dashboard()` 返回字段 | Task 5 | Task 6（API 响应）+ Task 8（前端 JS 取值） | ✅ |
| `seed_recipes` season 字段 | Task 2 | Task 3（_seasonal_pool 读取）+ Task 5（season_coverage 计算） | ✅ |

类型一致性全部通过。

---

**Plan complete.** M4.1 自动运营层，9 个 Task（3 后端模块 + 1 数据模型 + 1 seed 扩展 + 1 recipe_match 修改 + 1 API + 3 前端 + 1 回归），TDD 流程，每 Task 独立 commit。
